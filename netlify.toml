# Конфиг деплоя Netlify (Git- или CLI-деплой — чтобы попала serverless-функция).
# output: 'export' → публикуем готовую папку out/ + бандлим функции из netlify/functions.
[build]
  command = "npm run build"
  publish = "out"
  functions = "netlify/functions"
